# CngEditorSdk – iOS Native Stub

This directory is reserved for future native iOS implementations (e.g., GPU-accelerated filters, hardware video encoding/decoding via AVFoundation).

The current SDK version is pure React Native (JS/TS) with no native code required.
